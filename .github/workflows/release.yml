name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            artifact: Traumader-mac
          - os: windows-latest
            artifact: Traumader-windows
          - os: ubuntu-latest
            artifact: Traumader-linux
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller pillow

      - name: Build executable
        run: pyinstaller traumader.spec

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: |
          cd dist
          zip -r ../${{ matrix.artifact }}.zip Traumader

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: Compress-Archive -Path dist/Traumader.exe -DestinationPath ${{ matrix.artifact }}.zip

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: '*.zip'
          body: |
            ## Traumader herunterladen

            Lade das Paket für dein System herunter und folge der [Anleitung für Freunde](../blob/main/ANLEITUNG-FUER-FREUNDE.md).

            - **Mac:** `Traumader-mac.zip`
            - **Windows:** `Traumader-windows.zip`
            - **Linux:** `Traumader-linux.zip`
